# 仕様書: ボタンとダイヤル

全て透明背景。ボタン3種は9スライス（左96 / 右96 / 上72 / 下88）。
**文字は入れないでください。** FOLD / CALL / RAISE の文字はコード側で描きます。

## 共通構造（完成見本の実測）

1. **太い金のリムが上下にある。** 特に下端のリムが明るい
2. 面は上が明るく下が暗い縦グラデ
3. 角丸は大きめ（幅の18%程度）

| ファイル | サイズ | 上端リム | 面の上 | 面の下 | 下端リム |
|---|---|---|---|---|---|
| `btn-fold.png` | 864×660 | `#BC854F` | `#B82915` | `#580C01` | `#EEC89A` |
| `btn-call.png` | 880×660 | `#E6D6B7` | `#0D7CE0` | `#012B6A` | `#FEE0C8` |
| `btn-raise.png` | 908×660 | `#F6D5B5` | `#FED4A0` → `#F09A33` | `#8A4B0F` | `#FFF9E8` |

**CALLだけ**、面の最上部に `#64CFFF` のガラス質の鏡面ハイライトが乗ります。これが効いています。

## `dial.png` — ベット額ダイヤル

- **792 × 792 px**（実使用 198×198）／正円／9スライスなし
- **紺の円盤を厚い金環が囲む**構造
- 円盤: `#020C24` → `#00060E`。中心はほぼ黒に近い紺
- 金環: 上端 `#FFFFE1`、下端 `#FFFFEB`。かなり明るい
- 環の上に方位磁針状の飾り（完成見本 x714-910, y1451-1649 に実物）
- 中央に数字が乗るので、中心部は装飾を入れず平坦に
