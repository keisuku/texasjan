# 画像生成AIへの発注書 — MAHJONG HOLD'EM

---

# ★ round 4（最新の依頼）— 「スキン1式」を丸ごと作る

**発注の単位が変わりました。** これまでは「ボタン1点」「牌1点」という部品単位でしたが、
これからは **スキン1式** が単位です。1式がそのまま商品になります。

## 何が変わったのか

見た目が**差し替え可能なスキン**になりました。カタログは **`skins/manifest.js`** の1ファイルです。
実装側（`index.html`）はこのファイルしか読みません。

つまり **既存の絵を上書きする必要がもう無い** ということです。
新しい絵は `skins` 配列に1件足すだけで、選択肢として増えます。前の絵は残ります。
「どっちが正解か」で止まる必要がなくなりました。

## スキン1式とは

`skins/manifest.js` の `slots` にカテゴリとロールが定義されています。

| スロット | ロール |
|---|---|
| `tile` | `tile-body-flat` `tile-body-upright` `tile-back-flat` |
| `arena` | `arena` |
| `frame` | `frame-gold-plate` `frame-gold-ribbon` `frame-seat-plate` `frame-group` `btn-fold` `btn-call` `btn-raise` `dial` |
| `chip` | `chip-single` `chip-stack` |
| `effect` | `fx-glow` `fx-sparkle` |
| `character` | `character` |
| `nameplate` | `nameplate` |

**1つの世界観で全スロットを通して作ってください。** 部品ごとに空気が違うと、
並べた瞬間に破綻します。逆に統一されていれば、それ自体が商品価値になります。

## 追加の手順

1. 画像を `assets/skins/<スキンID>/` に置く（例 `assets/skins/night/btn-call.png`）
2. `skins/manifest.js` の `skins` 配列に追記する

```js
{ slot:"frame", id:"night", name:"夜想", note:"深夜のラウンジ",
  owned:true, swatch:"linear-gradient(160deg,#6A5BC4,#140F30)",
  assets:{
    "btn-call":"./assets/skins/night/btn-call.png",
    /* … ロールを埋めていく。埋めなかったロールはCSS手描きに落ちます … */
  } },
```

3. `tests/skins.html` をブラウザで開く。カタログの整合が自動判定されます

**既存の項目は書き換えないでください。追記だけしてください。**

## 寸法・光源・色

これまでと同じです。`art/ui-geometry.md` と `art/art-bible.md` に従ってください。
寸法（何pxで作るか）は `assets/ui/MANIFEST.json` の `size` をそのまま使ってください。

## 厳守事項

- **`index.html` を編集しないでください。** 実装側が同じファイルを編集中で、衝突します
- `assets/ui/*.png` `assets/ui/svg/*.svg` `assets/tiles/` は**既に使われている商品**です。変更しないでください
- 触ってよいのは `assets/skins/**` と `skins/manifest.js` への**追記**だけです
- 全ロールで光源は左上（315°）に統一

## 今いちばん欲しいもの

1. **キャラクターの立ち絵**（`character` ロール）。ロビーの左半分が空いています。
   縦長・背景透過・腰から上。ここが埋まるとロビーが一気に製品に見えます
2. **タイトルロゴ**（`assets/ui/logo-title.png`）。今はWebフォントで組んでいます
3. スキン1式（`night`「夜想」を想定。`skins/manifest.js` に未所持で予約済み）

---

# round 3（完了）— UIフレームをSVGで構築する

**これが今お願いしたい作業です。** 画像生成ではなく **SVGを書く作業** です。

## なぜSVGなのか

ここで必要な「完璧」は絵画的な巧さではなく **座標の正確さ** です。
Golden Visual のボタンの角丸を実測すると FOLD 53px / CALL 47px / RAISE 49px と
同一部品なのにばらついており、round 1・2 で生成した画像も面の中心のずれが
btn-fold 19px / btn-call **97px** / btn-raise 60px とバラバラでした。
1点ずつ見れば悪くなくても、並べた瞬間に「揃っていない」ことが安っぽさとして出ます。

画像生成モデルは1枚ずつ独立に描くのでこの一貫性を原理的に持てません。
SVGなら座標をそのまま書けるので、この弱点が消えます。

## 読むもの

1. **`art/ui-geometry.md`** — 全部品が共有する数値。角丸比・縁の5層・光源角度。**ここが核心**
2. **`art/specs/ui-frames.md`** — 部品ごとの寸法と色
3. **`assets/ui/svg/MANIFEST.json`** — 納品先の一覧

## 厳守事項

- **`index.html` を編集しないでください。** 実装側が同じファイルを編集中で、衝突します。
  納品は `assets/ui/svg/*.svg` のみ
- `assets/ui/*.png` と `assets/tiles/` も変更しないでください
- **牌に関する作業は不要です。** 牌面はCC0の正規資産（`assets/tiles/`）で完了済み
- **縁は必ず5層。** 2段グラデーションは不可。ここを省くと平坦に見えます
- 全部品で同一の角丸比・同一の光源角度（315°）
- 文字を一切入れない
- 通常/押下 は別ファイルにせず、1つのSVG内のレイヤー（`#gloss` `#press`）で作る

## 対象7点

`btn-fold` `btn-call` `btn-raise` `frame-gold-plate` `frame-seat-plate`
`frame-gold-ribbon` `frame-group`

`dial`・チップ・FXは絵の豊かさが効く部品なので、生成済みPNGのままにします。作り直し不要です。

## 検査

`art/verify-ui.html` をブラウザで開くと、viewBox・角丸比・縁の層数・光源の向き・必須IDを
自動判定します。落ちた項目は実測値と目標値を並べて表示するので、そのまま修正指示に使えます。

納品されたSVGは実装側でPNG版と同一条件のスクリーンショットで並べ、良いほうを採用します。

---

# 以下は round 1–2 の記録（牌面は完了済み。参考用）

**このリポジトリを見ているAIへ。あなたにお願いしたいのはゲーム用の画像素材の生成です。**

このプロジェクトはブラウザで動く麻雀 × ポーカーのゲームです。レイアウトとゲームロジックは
完成していますが、**画面上の絵が全てCSSで描かれた仮のもの**で、品質が全く足りていません。
目標水準は「ポーカーチェイス」相当の商用ゲームアートです。

あなたに作っていただきたいのは、**使い回しの効く素材**です。牌34種を34枚描くのではなく、
**面が無地の牌体を1枚**だけ最高品質で作ってもらい、それをコード側で136回使い回します。
牌の文字と絵柄はベクターで別途用意します。

---

## まず読むもの（この順に）

1. **`art/art-bible.md`** — 色・寸法・光源の実測値。**必ず守ってください。** 完成見本から
   1ピクセルずつ計測した数値で、推定や好みは入っていません
2. **`assets/ui/MANIFEST.json`** — 作るべき素材の一覧。ファイル名・寸法・アルファ要否の機械可読版
3. **`art/specs/*.md`** — 素材1点ごとの詳細仕様

## 見るべき参照画像

| ファイル | 何のためか |
|---|---|
| `claude-handoff/visual/golden-target.webp` | **完成見本。これが目標です。** 941×1672 |
| `claude-handoff/visual/sample-9pin.png` | 完成見本から切り出した**実物の牌1枚**（76×88）。牌の質感はこれが基準 |
| `claude-handoff/visual/traditional-tile-reference.jpeg` | 牌の意匠の基準 |
| `claude-handoff/visual/arena-with-characters.webp` | 実際に使っている背景。この上に素材が載ります |

`claude-handoff/visual/arena-clean.webp` は**使わないでください**。別生成の別レイアウトで、
卓の形が完成見本と一致しません。

---

## 最優先の1点

迷ったら**これだけ作ってください**。画面の印象の大半を決めます。

### `assets/ui/tile-body-flat.png` — 卓上の牌（面は無地）

- **260 × 292 px**（実使用 65×73 の4倍）／PNG／**背景は完全な透明**
- 象牙色の麻雀牌を、卓にやや伏せ気味に置いた角度で。**牌面には何も描かないでください**（無地）
- 光源は**左上**
- 上端に幅1px相当の鏡面ハイライト `#FFFDF1`
- 牌面は `#E1D1C2` を基準に、中央がわずかに明るい湾曲した反射
- **下端の11%（32px相当）が牌の厚みの面取り。** `#735F4D` から `#1A1410` へ階調をつけて落とす。
  ここが単色だと安っぽくなります
- 角丸は幅の6.2%（16px相当）
- 影を画像の外に落とさないこと。影は牌のシルエットの内側で表現します

詳細は `art/specs/tile-body.md`。

---

## 完成後の受け渡し

1. ファイル名は `assets/ui/MANIFEST.json` の `path` の通りにしてください（例 `tile-body-flat.png`）
2. 依頼者がダウンロードして `assets/ui/` に置き、コミットします
3. コード側は**素材が無ければ現行のCSSで動き、置かれた瞬間に自動で差し替わる**構造になっています。
   1点ずつ納品して構いません。全部揃うまで待つ必要はありません

## 守っていただきたいこと

- **寸法は MANIFEST.json の通りに。** 多少ずれても私（実装側）が校正しますが、アスペクト比は守ってください
- **背景は完全な透明に。** 透過が出せないツールの場合は、単色 `#FF00FF` の背景に描いてください。
  こちらで抜きます。白背景は牌が象牙色なので抜けません
- **光源は全素材で左上に統一。** これがばらつくと、並べた瞬間に破綻します
- **文字を描き込まないでください。** 「MAHJONG」等のロゴも不要です。文字は全てコード側で出します
- 9スライス指定のある素材は、四隅の装飾が中央部の引き伸ばしに巻き込まれない構図にしてください

## 受け入れ検査について

納品された素材は `art/verify.html` で自動チェックします（寸法・アルファの縁・左右対称性・
光源の一貫性・アートバイブルのパレットとの色差）。落ちた場合は「何がどれだけずれているか」を
数値でお返しするので、それを元に再生成してください。
